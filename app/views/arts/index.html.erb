


  <div id="particles-js"> 
  </div>
 <section class=" art-header">  
 <%= render partial: "shared/head"%>
</section>

<div class="container art-container"> 
 <% @arts.each_slice(12).to_a.each do |art| %>
 <div class="row ">
  <% art.each do |art| %>
  <div class="col-md-2 art-box" >
    <%= link_to image_tag(art.art_url(:thumb).to_s), art.art_url, target: :_blank %>
    <div class="title" > <%= art.title %></div>
    <div class="author"> <%= art.author %></div>
  </div>  
  <% end -%>
</div>
<% end %>
</div>




<%= render partial: "shared/footer"%>




<!-- <p id="notice"><%= notice %></p>

<h1>Arts</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Author</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @arts.each do |art| %>
      <tr>
        <td><%= art.title %></td>
        <td><%= art.author %></td>
        <td><%= link_to 'Show', art %></td>
        <td><%= link_to 'Edit', edit_art_path(art) %></td>
        <td><%= link_to 'Destroy', art, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Art', new_art_path %>
 -->